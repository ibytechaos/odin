# Odin Framework Configuration Example
# Copy this file to .env and fill in your values

# ============================================
# General Settings
# ============================================
ODIN_ENV=development
ODIN_DEBUG=true
ODIN_LOG_LEVEL=INFO

# ============================================
# LLM Provider Settings
# ============================================
# Choose LLM provider: openai, anthropic, or azure
ODIN_LLM_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# Optional: Use custom OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic Configuration
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Azure OpenAI Configuration
# AZURE_OPENAI_API_KEY=your-azure-key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================
# Agent Backend Configuration
# ============================================
# Choose agent backend: crewai, langgraph, custom
# crewai = Multi-agent orchestration (default, recommended)
# langgraph = Complex state graphs (not yet implemented)
# custom = Your custom agent implementation
ODIN_AGENT_BACKEND=crewai

# Agent Metadata
ODIN_AGENT_NAME=odin_agent
ODIN_AGENT_DESCRIPTION=AI assistant powered by Odin framework

# Custom Agent (only if using custom backend)
# Path to custom agent class (e.g., "my_agents.MyCustomAgent")
# ODIN_CUSTOM_AGENT_PATH=my_agents.CustomAgent

# ============================================
# Tracing & Monitoring
# ============================================
# OpenTelemetry
OTEL_ENABLED=true
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=odin

# ============================================
# Conversation Persistence (Checkpointer)
# ============================================
# Choose checkpointer type: memory, sqlite, postgres, redis
# memory: No persistence (default, data lost on restart)
# sqlite: File-based persistence (recommended for development)
# postgres: Database persistence (recommended for production)
# redis: In-memory persistence with optional disk backup
ODIN_CHECKPOINTER_TYPE=memory

# Optional: Custom checkpointer connection string
# For SQLite: ODIN_CHECKPOINTER_URI=./data/checkpoints.db
# For Postgres: ODIN_CHECKPOINTER_URI=postgresql://user:pass@localhost:5432/odin
# For Redis: ODIN_CHECKPOINTER_URI=redis://localhost:6379/1

# ============================================
# Storage Backend
# ============================================
# Redis
ODIN_REDIS_URL=redis://localhost:6379/0

# SQLite
ODIN_SQLITE_PATH=./data/odin.db

# PostgreSQL (optional)
# ODIN_POSTGRES_URL=postgresql://user:pass@localhost:5432/odin

# ============================================
# Protocol Settings
# ============================================
# HTTP Server
HTTP_HOST=0.0.0.0
HTTP_PORT=8000

# MCP Server
MCP_ENABLED=true
MCP_PORT=8001

# A2A Settings
A2A_ENABLED=false
A2A_PORT=8002

# ============================================
# Plugin Settings
# ============================================
PLUGIN_AUTO_DISCOVERY=true
PLUGIN_DIRS=./plugins

# ============================================
# Security
# ============================================
API_KEY=your-secret-api-key
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================
# Performance
# ============================================
# Token limit and cost tracking
TOKEN_LIMIT_PER_REQUEST=100000
ENABLE_SEMANTIC_CACHE=true

# Rate limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60
